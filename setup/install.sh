#!/bin/bash
# Trels Linux Installer Setup Script placeholder

echo "Starting Trels installation for Linux..."
echo "This script will eventually install the Trels Go binary as a systemd service."

# Example:
# cp ../backend/trels /usr/local/bin/
# cp trels.service /etc/systemd/system/
# systemctl daemon-reload
# systemctl enable --now trels

echo "Installation complete."
